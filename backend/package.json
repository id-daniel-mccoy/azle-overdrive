{
    "scripts": {
        "setup": "npm install && dfx start --clean --background && dfx deploy && dfx stop && echo 'Setup complete!'"
    },
    "dependencies": {
        "azle": "0.25.0"
    },
    "devDependencies": {
        "jest": "^29.7.0",
        "ts-jest": "^29.3.2"
    }
}