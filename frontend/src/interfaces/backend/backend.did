service: () -> {
    createUser: (text) -> (text);
    getAllData: () -> (vec record {text; record {bio:text; username:text}}) query;
    getAllUsers: () -> (vec record {bio:text; username:text}) query;
    who: () -> (text) query;
}
